development: &global_settings
  host: 127.0.0.1
  database: scribe-development
  port: 27017

test:
  database: scribe-test
  <<: *global_settings

production:
  sessions:
    default:
      uri: <%= ENV['MONGOLAB_URI'] %>

  <<: *global_settings
